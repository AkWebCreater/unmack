


<style>
@media (min-width:768px){

    .slide-wrapper{
display: flex;
min-height: 300px;
text-align: center;
transition: all 0.4s ease-in-out;
position: relative;
}
.banner-slide{
    flex: 1;
}
.banner-slide.active{
    background-color:rgba(0,0,0,0.4);
}
.banner-slide.active * {
opacity: 0.7;
}
.slide-wrapper .banner-slide{
    border-left: 1px solid #fff;
    opacity: 1;
}
.slide-wrapper .banner-slide:first-child{
    border-left:0;
}
.slide-content-wrapper{
    padding: 15px;
}
.slide-content-wrapper *{
    color: #fff;
    opacity: 0.8;

}
.slide-content-wrapper{
    transform: translateY(20px);
    transition: all 0.4s ease-in-out;
}
.slide-content-wrapper.active{
transform: translateY(-20px);
    opacity: 1;
}
.banner-image{
    position: absolute;
    width: 100%;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    transition: all 0.4s ease-in-out;
}
.banner-image{
    opacity: 0.1;
}
.banner-image.active{
    opacity: 1;
}

}
</style>
<script>
$(document).ready(function(){
    // adding slider to mobile and tab view
if($(window).width() < 768){

    $('.slide-wrapper').slick({
          arrows: false,
          infinite: true,
          dots:true,
          speed: 300,
          nextArrow: '<span  class="slick-next"><i class="fa fa-angle-right" aria-hidden="true"></i></span>',
          prevArrow: '<span  class="slick-prev"><i class="fa fa-angle-left" aria-hidden="true"></i></span>',
          slidesToShow: 1,
          slidesToScroll: 1
      });
}
// changing bg on hover over slide
$('.banner-slide').hover(function(){
    var bg = $(this).attr('data-bg');
    $(this).addClass('active');
    $(this).find('.slide-content-wrapper').addClass('active')
    $('.slide-wrapper').find('.banner-image').css('background-image','url(' + bg + ')').addClass('active')
    
},function(){
    $(this).removeClass('active');
    $(this).find('.slide-content-wrapper').removeClass('active') 
    $('.slide-wrapper').find('.banner-image').removeClass('active')
})
})

</script>



  <div class="">
      <div class="slide-wrapper">
          <div class="banner-image"></div>
          {% for block in section.blocks %}
          <div class="banner-slide" 
          {% if block.settings.slide-bg != blank %}
          data-bg="{{block.settings.slide-bg | img_url: 'master'}}"{% endif %}
          >
        <div class="slide-content-wrapper">
            <h2 class="slide-title">{{block.settings.title}}</h2>
            <p class="slide-desc">
                {{block.settings.description}}
            </p>
        </div>
        </div>
              
          {% endfor %}
      </div>
  </div>
  


{% javascript %}
  
{% endjavascript %}
{% schema %}
{
  "name": "Home Banner",
  "class": "home-banner-section",
  "settings": [
{
    "type": "text",
    "id": "title",
    "label": "Section",
    "default": "Home Banner"
}
  ],
  "max_blocks": 5,
  "blocks":[
  {      
"name": "slide",
"type": "slide",
"settings": [
    {
        "type": "text",
        "id": "title",
        "label": "title"
    },
    {
        "type": "image_picker",
        "id": "slide-bg",
        "label": "slide image"
    },
    {
        "type": "text",
        "id": "description",
        "label": "Slide Description"
    }
]
  }
  ],
  "presets": [
      {
          "category": "slider",
          "name":"Banner Slider"
      }
  ]
}
{% endschema %}
